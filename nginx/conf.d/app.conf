upstream backend {
    server api:8000;
}

upstream frontend {
    server vue:8080;
}

server {
    listen 80;
    
    location / {
        proxy_pass http://frontend;
    }

    location /api {
        proxy_pass http://backend;

        client_max_body_size 64M;
    }
}
